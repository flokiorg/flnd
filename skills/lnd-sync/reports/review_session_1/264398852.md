# Commit Review - 264398852

## Commit Info
- **Hash:** 2643988524028566b78a8ad81fb7afc79c0a3ebe
- **Message:** make: fix linter in git worktrees

## Rebranding Check
- [x] No rebranding needed (`lnd-tools` and `lnd-go-lint-cache` are used as-is in `flnd`'s Makefile).

## Technical Integrity
- [x] Applies cleanly
- [x] Makefile logic is sound
- [x] No syntax errors introduced

## AI Assessment
- [x] No botched search/replace
- [x] Context preserved

## Notes
- Modifies `Makefile` to mount the main `.git` directory when running `make lint` inside a git worktree.
- This allows `golangci-lint`'s `new-from-rev` to work correctly in worktrees.
